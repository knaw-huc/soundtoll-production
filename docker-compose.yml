version: '2'

networks:
  sont_network:
    external:
      name: nginxproxynetwork

services:
  elastic:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.5.2
    container_name: soundtoll_es
    restart: always
    environment:
      - discovery.type=single-node
    expose:
      - 9200
    networks:
      - sont_network
    volumes:
      - /data/docker/volumes/sont_data/_data:/usr/share/elasticsearch/data

  sont_db:
    image: mysql:5.7
    container_name: soundtoll_mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    expose:
      - 3306
    environment:
      MYSQL_USER: user
      MYSQL_PASSWORD: user
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: default_schema
    volumes:
      - /data/docker/volumes/sont_db/_data/mysql:/var/lib/mysql
    ports:
      - 3306:3306
    networks:
      - sont_network

  sont:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: soundtoll_application
    restart: always
    environment:
      - PMA_HOST=sont_db
      - PMA_PORT=3306
      - PMA_ARBITRARY=1
      - VIRTUAL_HOST=soundtoll.sd.di.huc.knaw.nl
    links:
      - elastic
      - sont_db
    ports:
      - 8080:80
    networks:
      - sont_network